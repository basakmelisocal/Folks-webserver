# from jsonschema import validate, ValidationError, SchemaError
# from validation_scheme import validation_scheme
#
# def validate_request(args, method, url):
#     if validation_scheme.get(url, None):
#         validate(args, validation_scheme[method][url])
